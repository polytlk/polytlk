enum AUTH_ENDPOINTS {
  // Meta
  CONFIG = '/config',

  // Auth: Basics
  SESSION = '/auth/session',

  // Auth: Social
  REDIRECT_TO_PROVIDER = '/auth/provider/redirect',
}

export { AUTH_ENDPOINTS };

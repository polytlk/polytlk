{{- if .Values.gateway.tls.enabled }}
apiVersion: v1
kind: Secret
metadata:
    name: {{ .Values.gateway.tls.secretName }}
type: kubernetes.io/tls
data:
    tls.crt: {{ .Values.gateway.tls.cert }}
    tls.key: {{ .Values.gateway.tls.key }}
{{- end }}
